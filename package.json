{
  "name": "anarix",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:dev": "cd frontend && npm run build --mode development",
    "typecheck": "cd frontend && npx tsc -b"
  }
}
