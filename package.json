{
  "name": "anarix",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun --cwd frontend run dev",
    "build": "bun --cwd frontend run build",
    "build:dev": "bun --cwd frontend run build:dev",
    "typecheck": "bun --cwd frontend x tsc -b"
  }
}
