{
  "framework": "vite",
  "installCommand": "yarn install --frozen-lockfile=false --ignore-engines --production=false",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
